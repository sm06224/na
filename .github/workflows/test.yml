name: test

# 作品のうちロジックを持つもの（genesis の人工生命コア）を
# ヘッドレスで検証する。ブラウザ不要、依存ゼロ、Node 標準のテストランナー。
on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  genesis:
    name: genesis — artificial life core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run the universe in a test tube
        working-directory: works/genesis
        run: node --test tests/*.test.js

  shi:
    name: shi — civilization core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run eight centuries of history headless
        working-directory: works/shi
        run: node --test tests/*.test.js

  ban:
    name: ban — shift scheduler core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Solve a month of rosters headless
        working-directory: works/ban
        run: node --test tests/*.test.js

  koto:
    name: koto — emergent language core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Grow a language from nothing, headless
        working-directory: works/koto
        run: node --test tests/*.test.js

  uta:
    name: uta — emergent music core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Let six centuries of songs be sung, headless
        working-directory: works/uta
        run: node --test tests/*.test.js

  sonae:
    name: sonae — stockpile calculator core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the math a family will rely on
        working-directory: works/sonae
        run: node --test tests/*.test.js

  kanade:
    name: kanade — shared instrument core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Guarantee that no two fingers can clash
        working-directory: works/kanade
        run: node --test tests/*.test.js

  koke:
    name: koke — garden as a function of time
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Fast-forward ten years of moss
        working-directory: works/koke
        run: node --test tests/*.test.js

  ori:
    name: ori — loom core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Weave the letter again and compare every pixel
        working-directory: works/ori
        run: node --test tests/*.test.js

  san:
    name: san — a computer born from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Boot the machine, compile, and run every ROM headless
        working-directory: works/san
        run: node --test tests/*.test.js

  hari:
    name: hari — way-back needle core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the spherical math a walker will rely on
        working-directory: works/hari
        run: node --test tests/*.test.js

  kitsune:
    name: kitsune — GPS treasure hunt core
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Fold a course, build a QR, and walk the hunt to the end
        working-directory: works/kitsune
        run: node --test tests/*.test.js

  hoshi:
    name: hoshi — a sky that names itself
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Scatter a sky from a seed and read the letter in the lead star
        working-directory: works/hoshi
        run: node --test tests/*.test.js

  kaminari:
    name: kaminari — lightning by dielectric breakdown
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Grow a bolt from a seed and read the letter in its flash
        working-directory: works/kaminari
        run: node --test tests/*.test.js

  hinata:
    name: hinata — where the sun reaches
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Track the sun by spherical astronomy and check a window's light
        working-directory: works/hinata
        run: node --test tests/*.test.js

  wari:
    name: wari — settle up with the fewest transfers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Split, balance, and minimise the transfers headless
        working-directory: works/wari
        run: node --test tests/*.test.js

  kutsu:
    name: kutsu — a labyrinth no one designs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Dig a dungeon from a seed and walk it headless
        working-directory: works/kutsu
        run: node --test tests/*.test.js

  tane:
    name: tane — plants grown from a seed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Grow a plant from a seed, down to the last leaf, headless
        working-directory: works/tane
        run: node --test tests/*.test.js

  kuji:
    name: kuji — a draw no one can rig
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Hash known vectors, then draw and verify a fair lottery headless
        working-directory: works/kuji
        run: node --test tests/*.test.js

  nami:
    name: nami — a pond you can touch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Drop a ripple and check the water stays stable and symmetric
        working-directory: works/nami
        run: node --test tests/*.test.js

  sou:
    name: sou — a year becomes a layer; a cliff remembers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Lay down deep time from a seed and read the cliff headless
        working-directory: works/sou
        run: node --test tests/*.test.js

  jin:
    name: jin — a tactics RPG born from a seed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Generate a campaign from a seed and fight every chapter headless
        working-directory: works/jin
        run: node --test tests/*.test.js

  yuki:
    name: yuki — a snowflake is a letter from the sky
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Grow a snowflake from a seed; check it is six-fold and grows headless
        working-directory: works/yuki
        run: node --test tests/*.test.js

  hibiki:
    name: hibiki — resonance born from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Cast resonant objects from a seed and check the struck waveforms headless
        working-directory: works/hibiki
        run: node --test tests/*.test.js

  dandori:
    name: dandori — get every dish hot at once
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Schedule a meal backwards and check no resource is double-booked headless
        working-directory: works/dandori
        run: node --test tests/*.test.js

  sora:
    name: sora — a space megademo from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the timeline, the 3D math and the in-scale soundtrack headless
        working-directory: works/sora
        run: node --test tests/*.test.js

  koe:
    name: koe — a voice born from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Synthesize vowels and check pitch periodicity and formant peaks headless
        working-directory: works/koe
        run: node --test tests/*.test.js

  hi:
    name: hi — a stone that remembers the names
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the append-only ledger and the deterministic engraving headless
        working-directory: works/hi
        run: node --test tests/*.test.js

  akashi:
    name: akashi — proof, and a token of identity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the fingerprint's determinism, avalanche and the symmetric sigil headless
        working-directory: works/akashi
        run: node --test tests/*.test.js

  kage:
    name: kage — a theatre of shadows
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Cast shadows from a lamp; check the projection, penumbra and flicker headless
        working-directory: works/kage
        run: node --test tests/*.test.js

  kei:
    name: kei — a calculator you can read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Reckon units, currencies, percents and back-references headless
        working-directory: works/kei
        run: node --test tests/*.test.js

  nazo:
    name: nazo — the Enigma, rebuilt from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Cipher against historical vectors; check reciprocity and double-stepping headless
        working-directory: works/nazo
        run: node --test tests/*.test.js

  han:
    name: han — Reversi, a mind built from nothing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check the rules, then prove the AI never loses to random and deeper beats shallower
        working-directory: works/han
        run: node --test tests/*.test.js

  kyo:
    name: kyo — games that invent themselves, and judge their own fun
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Anchor the engine on solved tic-tac-toe, then forge rulesets and score their fun headless
        working-directory: works/kyo
        run: node --test tests/*.test.js

  hakana:
    name: hakana — the fleeting colours of a thin film
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Prove the colour science and thin-film interference from white point to black film headless
        working-directory: works/hakana
        run: node --test tests/*.test.js

  madara:
    name: madara — the coats that pattern themselves
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Diffuse a Turing pattern from a seed and prove it grows spots, stripes, mazes and holes headless
        working-directory: works/madara
        run: node --test tests/*.test.js

  studio:
    name: studio — Mermaid diagram editor
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Parse Mermaid gantt and flowchart, resolve a schedule, auto-layer a graph, round-trip the layout and bundle a single HTML headless
        working-directory: studio
        run: node --test tests/*.test.js

  hotaru:
    name: 蛍 — do the fireflies still glow?
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # 蛍にテストの檻は似合わない。光るかどうかだけ、そっと確かめる。
      - name: Three frames of June night
        run: HOTARU_ITSUDEMO=1 HOTARU_FRAMES=3 ./蛍 > /dev/null
